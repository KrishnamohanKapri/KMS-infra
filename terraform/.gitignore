# Terraform files
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
*.tfvars
!terraform.tfvars.example
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Backup files (may contain secrets)
*.backup
*.backup.*
terraform.tfvars.backup.*

# Secrets and keys
*.pem
*.key
!*.pub

# DigitalOcean API tokens (in terraform.tfvars)
# terraform.tfvars is already covered by *.tfvars above

# Environment files
.env
.env.local
.env.*.local

# Additional secret patterns (optional but recommended)
secrets/
*.secret
*.token
credentials/
*.credentials